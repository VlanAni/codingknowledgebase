#### `CREATE` 

Команда `CREATE` создаёт базу данных 
### `INSERT`

Команда `INSERT` позволяет **вставлять** данные в таблицу

При использовании команды мы указываем таблицу и поля, в которую вставляем значения 

