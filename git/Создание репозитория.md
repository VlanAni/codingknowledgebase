Команда `git init` инициализирует пустой репозиторий, создавая в директории папку `.git` c поддиректориями `objects`, `refs/heads`, `refs/objects` 

Внутри репозитория также создаётся ветка, в которой нет коммитов 
