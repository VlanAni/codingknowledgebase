Для фиксации изменений используется команда `git commit`

Команда `git commit`:
1. берёт **изменения**, вписанные в индекс
2. создаёт из них **коммит**
3. **подписывает** коммит заданным сообщением
4. новый коммит становится дочерним для `HEAD` данной ветки 

